On a box that is used as a frontend, as root:

    # perl mdd.pl

On a box that is used only as a backend, as root:

    # perl mdd.pl --backend

    Then run /usr/bin/mdd as a non-root user and arrange for it to be run at boot (as a non-root user).
