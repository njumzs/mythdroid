On a box that is used as a frontend, as root:

    perl mdd

On a box that is used only as a backend, as root:

    perl mdd --backend

    (Then run /usr/bin/mdd and arrange for it to be run at boot).
